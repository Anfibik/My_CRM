import React from 'react';
import { Box, Typography } from '@mui/material';

const MyEvents = () => (
  <Box mt={2}>
    <Typography variant="body2" color="textSecondary">
      Здесь заглушка для «Мои события»
    </Typography>
  </Box>
);

export default MyEvents;
