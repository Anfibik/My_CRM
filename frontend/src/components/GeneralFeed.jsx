import React from 'react';
import { Box, Typography } from '@mui/material';

const GeneralFeed = () => (
  <Box mt={2}>
    <Typography variant="body2" color="textSecondary">
      Здесь заглушка для «Общая лента»
    </Typography>
  </Box>
);

export default GeneralFeed;
